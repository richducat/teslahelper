// Runtime overrides for Tesla Helper.
// Populate window.APP_ENV with any environment-specific overrides. This file exists
// to avoid 404s in production when no dynamic runtime config is provided.
window.APP_ENV = window.APP_ENV || {};
